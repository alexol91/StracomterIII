[gd_resource type="StandardMaterial3D" load_steps=2 format=3]

; Mobiliario: `mesa.png`, la textura del modelo de mesa
; (ResourceManager.cc:784). En el original cada mueble traía la suya; aquí una
; sola superficie PROP los cubre a todos hasta que haya modelos por tipo.

[ext_resource type="Texture2D" path="res://assets/textures/chutaos/mesa.png" id="Tex"]

[resource]
albedo_color = Color(1, 1, 1, 1)
albedo_texture = ExtResource("Tex")
uv1_scale = Vector3(1, 1, 1)
roughness = 1.0
metallic = 0.0
metallic_specular = 0.0
