[gd_resource type="StandardMaterial3D" load_steps=3 format=3]

; Mobiliario: melamina con grano. Un punto más oscuro que las paredes para que
; las coberturas se distingan del fondo: si el jugador no ve dónde parapetarse,
; el sistema de coberturas de la IA es un lujo inútil. Hay un test que mide esa
; diferencia sobre el color medio de la textura, no sobre el tinte.

[ext_resource type="Texture2D" path="res://assets/textures/modern/prop_albedo.png" id="Albedo"]
[ext_resource type="Texture2D" path="res://assets/textures/modern/prop_normal.png" id="Normal"]

[resource]
albedo_color = Color(0.46, 0.44, 0.42, 1)
albedo_texture = ExtResource("Albedo")
normal_enabled = true
normal_texture = ExtResource("Normal")
normal_scale = 0.5
roughness = 0.7
metallic = 0.05
metallic_specular = 0.3
uv1_scale = Vector3(1, 1, 1)
uv1_triplanar = true
uv1_world_triplanar = true
